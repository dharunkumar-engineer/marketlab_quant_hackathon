services:
  - type: web
    name: marketlab-quant
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 --workers 1 app:app
    healthCheckPath: /api/health
